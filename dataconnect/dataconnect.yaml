specVersion: "v1"
serviceId: "flashfit"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flashfit-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
